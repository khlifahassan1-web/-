# تحويل حسابات المعمل إلى تطبيق APK حقيقي (كل الخطوات من الهاتف)

## الخطوة 1: استضافة الملفات على رابط دائم (GitHub Pages)

1. افتح github.com من متصفح الهاتف وسجّل حساب (مجاني) إذا لم يكن لديك.
2. اضغط **+** أعلى الصفحة ← **New repository**.
3. اسم المستودع: `lab-accounts` (أو أي اسم) ← اجعله **Public** ← **Create repository**.
4. داخل المستودع: **Add file** ← **Upload files**.
5. ارفع كل الملفات التالية دفعة واحدة (اختر "اختيار من الملفات"):
   - `index.html`
   - `manifest.json`
   - `service-worker.js`
   - مجلد `icons` بكامل محتواه (`icon-192.png`, `icon-512.png`, `icon-512-maskable.png`)
6. اكتب رسالة أي شيء ← **Commit changes**.
7. من إعدادات المستودع: **Settings** ← **Pages** (بالقائمة الجانبية).
8. تحت **Branch** اختر `main` والمجلد `/ (root)` ← **Save**.
9. انتظر دقيقة، سيظهر لك رابط بالشكل:
   ```
   https://اسم_حسابك.github.io/lab-accounts/
   ```
   هذا هو رابطك الدائم — افتحه وتأكد أن التطبيق يعمل ويطلب تسجيل الدخول بشكل طبيعي.

## الخطوة 2: تحويله إلى APK عبر PWABuilder

1. من متصفح الهاتف افتح: **pwabuilder.com**
2. الصق رابط GitHub Pages من الخطوة السابقة في المربع، واضغط **Start**.
3. ستفحص الأداة التطبيق (Manifest + Service Worker) — يفترض تظهر علامتا ✅ خضراوين.
4. اضغط **Package For Stores** (أو **Publish**).
5. اختر **Android**.
6. اترك الإعدادات الافتراضية (أو غيّر اسم الحزمة Package ID إذا أردت، مثل `com.labaccounts.app`).
7. اضغط **Generate** / **Download** — ستحصل على ملف مضغوط يحتوي APK (وربما AAB).
8. فك الضغط من مدير الملفات بالهاتف، وستجد ملف `.apk` جاهزًا للتثبيت.

## الخطوة 3: تثبيت الـ APK على هاتفك

1. افتح ملف الـ APK من مدير الملفات.
2. إذا ظهرت رسالة "غير مسموح بالتثبيت من مصادر غير معروفة" ← اذهب للإعدادات المقترحة وفعّل السماح لمتصفحك/مدير الملفات، ثم عد وثبّت.
3. بعد التثبيت ستجد أيقونة "حسابات المعمل" في قائمة تطبيقاتك — تفتح بدون شريط متصفح، مثل أي تطبيق عادي.

## ملاحظات مهمة
- الـ APK الناتج هو "غلاف" حول نفس صفحة الويب (WebView) — يحتاج اتصال إنترنت ليعمل لأنه يتصل بقاعدة بياناتك على Supabase مباشرة، تمامًا كما يعمل الآن في المتصفح.
- أي تحديث تعمله لاحقًا على `index.html`، فقط ارفعه من جديد على GitHub (Upload files بنفس المكان) وسيتحدث تلقائيًا — لا تحتاج تُنشئ APK جديد كل مرة إلا إذا غيّرت الأيقونة أو الاسم.
- إذا أردت نشره لاحقًا على Google Play رسميًا، ستحتاج حساب مطوّر (رسوم لمرة واحدة تقريبًا 25$) ورفع ملف AAB بدل APK — يمكن لـ PWABuilder توليد الاثنين.
- إذا واجهتك أي مشكلة أو رسالة خطأ في أي خطوة، أرسل لي صورة وسأحلها معك.
